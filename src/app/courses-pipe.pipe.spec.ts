import { CoursesPipePipe } from './courses-pipe.pipe';

describe('CoursesPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CoursesPipePipe();
    expect(pipe).toBeTruthy();
  });
});
